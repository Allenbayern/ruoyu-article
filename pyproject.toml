[project]
name = "ruoyu-film-daily"
version = "0.1.0"
description = "Offline controlled-production gates for the Ruoyu film article group"
requires-python = ">=3.14"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
